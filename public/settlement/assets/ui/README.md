# UI art seam

The current UI uses the same game palette and live activity icons as the renderer. Discovery, population, and resource panels remain text/data-driven so the information stays readable and accessible.
