name: Verify Johnny Chat Pages Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  pages-build:
    name: Pages-ready build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Build the Pages payload
        run: CF_PAGES=1 npm run build:pages

      - name: Verify routes and Pages file sizes
        run: node scripts/verify-pages-build.mjs
